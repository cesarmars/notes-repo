# Motivation

Cache is high speed memory storage that stores a subset of data in transiet way, memory that
can be obtain fast.

Caches allow us to 

- fetch memory/information faster because it is closer to the CPU (locality)
- get more power efficiency compared to RAM
- scale to multiple CPU'S
